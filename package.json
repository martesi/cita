{
  "name": "cita",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site --host 0.0.0.0",
    "dev:worker": "wrangler d1 migrations apply DB --local && wrangler dev",
    "check": "tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler d1 migrations apply DB --remote && wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  },
  "cloudflare": {
    "bindings": {
      "DB": {
        "description": "D1 database used to store temporary citation payloads. It is provisioned automatically by Deploy to Cloudflare."
      }
    }
  }
}
