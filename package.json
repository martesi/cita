{
  "name": "cita",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite site --host 0.0.0.0",
    "dev:worker": "wrangler dev",
    "check": "bun test && tsc --noEmit && wrangler deploy --dry-run --outdir dist",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@modelcontextprotocol/server": "2.0.0",
    "agents": "latest",
    "zod": "latest"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "vite": "latest",
    "wrangler": "latest"
  }
}
